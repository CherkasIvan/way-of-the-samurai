export enum ActiveRoutes {
  DEFAULT = '/',
  PROFILE = '/profile',
  DIALOGS = '/dialogs',
  NEWS = '/news',
  MUSIC = '/music',
  SETTINGS = '/settings',
  POSTS = '/posts',
  USERS = '/users',
	LOGIN = '/login',
}
